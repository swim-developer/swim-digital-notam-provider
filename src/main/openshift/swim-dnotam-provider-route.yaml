apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: swim-dnotam-provider-mtls
  namespace: swim-demo
  labels:
    app: swim-dnotam-provider
    app.kubernetes.io/part-of: swim-dnotam
  annotations:
    haproxy.router.openshift.io/timeout: 60s
spec:
  host: swim-dnotam-provider-mtls.apps.ocp4.masales.cloud
  to:
    kind: Service
    name: swim-dnotam-provider
  port:
    targetPort: https
  tls:
    termination: passthrough
    insecureEdgeTerminationPolicy: Redirect

